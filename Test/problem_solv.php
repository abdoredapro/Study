<?php 
    /* Day 1 [] */
    // count => GET array length
    // range() => Make array from start and and 
    /*
        return array_map(function ($el) use ($arr){
        return $el * count($arr);
            }, $arr);
        }
    */

    // array_sum => Return Sum of element array

    // implode() => Array to String 
    // explode() => String to Array 
    // substr(String, Start, Length) => Return part of a string






